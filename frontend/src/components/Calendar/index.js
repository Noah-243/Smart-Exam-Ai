/**
 * index.js – Calendar Component Export
 *
 * This file re-exports the Calendar component to simplify imports.
 * Instead of importing from "./Calendar/Calendar", other modules can just use:
 * import Calendar from "./Calendar"
 */ט
import Calendar from "./Calendar";

export default Calendar;
