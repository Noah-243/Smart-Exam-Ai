/**
 * index.js for SidebarLayout
 *
 * This file simplifies imports by re-exporting the SidebarLayout component.
 * Allows importing from the folder directly instead of specifying the file path.
 *
 * Example:
 * import SidebarLayout from "./SidebarLayout";
 */

import SidebarLayout from "./SidebarLayout";

export default SidebarLayout;
