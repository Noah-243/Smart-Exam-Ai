/**
 *
 * This file serves as a barrel export for the `Login` component.
 * 
 * Purpose:
 * --------
 * - Simplifies imports in other parts of the application by allowing:
 *   `import Login from '.../pages/Login'`
 *   instead of specifying the full path to `Login.jsx`.
 *
 * Dependencies:
 * -------------
 * - `Login` component (./Login)
 */
import Login from "./Login";

export default Login;
